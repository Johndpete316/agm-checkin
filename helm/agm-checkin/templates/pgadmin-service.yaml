apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pgadmin
  labels:
    {{- include "agm-checkin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: agm-pgadmin
  ports:
    - port: 80
      targetPort: 80
