apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
  labels:
    {{- include "agm-checkin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: agm-api
  ports:
    - port: 8080
      targetPort: 8080
