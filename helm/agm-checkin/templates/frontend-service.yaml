apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    {{- include "agm-checkin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: agm-frontend
  ports:
    - port: 80
      targetPort: 80
