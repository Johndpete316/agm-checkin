apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-postgres
  labels:
    {{- include "agm-checkin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: agm-postgres
  ports:
    - port: 5432
      targetPort: 5432
